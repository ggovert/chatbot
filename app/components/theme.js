import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    background: {
      default: '#f3f2e4',
    },
    primary: {
      main: '#fabc30',
      sidebar: '#f2e5c8',
      darker: '#e3e2cf',
      border: '#000',
      insideForm: '#eee',
    },
  },
});
export default theme;
